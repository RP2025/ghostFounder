# Copy to .env.local and adjust. .env.local is gitignored.

# URL of the FastAPI backend that the Next.js API routes proxy to
# (used server-side in app/api/*/route.ts). Point this at your deployed
# backend in production, e.g. https://ghostfounder-api.onrender.com
BACKEND_URL=http://localhost:8000
